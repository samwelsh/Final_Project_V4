@page
@model Final_Project.Pages.Cities.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>BRE Cities List</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.City[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.City[0].State)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.City[0].Zip)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.City) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.State)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Zip)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CityId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CityId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CityId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
